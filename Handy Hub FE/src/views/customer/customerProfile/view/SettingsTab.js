import React from 'react'

const SettingsTab = ({ userId }) => {
  return (
    <div>
      <h4>Account Settings</h4>
      <p>Settings for user ID: {userId}</p>
    </div>
  )
}

export default SettingsTab
